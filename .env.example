# Where the AI function lives (Netlify). Leave unset to run the app in demo mode.
EXPO_PUBLIC_API_URL=https://your-site.netlify.app/api
# Optional light gate; must match APP_KEY on the server. Not a secret — it ships in the app.
EXPO_PUBLIC_APP_KEY=
